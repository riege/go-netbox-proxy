apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-netbox-proxy.fullname" . }}
  labels:
    {{- include "go-netbox-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-netbox-proxy.selectorLabels" . | nindent 4 }}
